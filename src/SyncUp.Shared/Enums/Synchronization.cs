namespace SyncUp.Shared.Enums
{
    public enum SyncStatus
    {
        Unknown,
        OutOfSync,
        InSync
    }

    public enum Location
    {
        None,
        Agent,
        Server
    }
}
